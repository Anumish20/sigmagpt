# ---- SigmaGPT frontend config ----
# Dev: leave this UNSET — Vite proxies /api to http://localhost:8080.
# Prod (frontend on Vercel, backend hosted elsewhere): set the full backend API URL.
# VITE_API_URL=https://your-backend.example.com/api
